import React from "react";

const Sidebar_subcriptions_button = (props) => {
  return (
    <div className="home">
      <div className="butt_img">{props.logo}</div>
      <div className="home_text">{props.text}</div>
    </div>
  );
};

export default Sidebar_subcriptions_button;
